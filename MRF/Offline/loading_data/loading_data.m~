
fid = textread('urls.txt', '%s', 'delimiter', ',');

for i=1:1
    url = char(strtrim(fid(i,1)));   
    n = length(url);
    disp(url);
    filename = 'data'+string(i)+'.mat';
    outfilename = websave(filename,url,weboptions('ContentType','binary'));
    x = load(filename);
    save(x);
    disp(i);
end